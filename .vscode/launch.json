{
    "configurations": [
        {
            "type": "chrome",
            "name": "http://${this.name}",
            "request": "launch",
            "url": "http://${this.name}"
        }
    ]
}